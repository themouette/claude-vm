#!/bin/bash
# Network security host setup
# No host setup needed - mitmproxy runs inside each VM

echo "Network security setup..."
echo "✓ No host-side setup required (proxy runs in VM)"
echo ""
echo "Network security will be configured when the VM starts."
echo "Each VM runs its own isolated mitmproxy instance."
